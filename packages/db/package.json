{
  "name": "@scopeshield/db",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "private": true,
  "dependencies": {
    "@scopeshield/domain": "workspace:*"
  },
  "scripts": {
    "postinstall": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:generate": "prisma generate"
  }
}